Parsing: Reading a stream of tokens according to a grammar is called parsing.

Grammer Rules:
  Expression:
    Term
    Term "+" Expression 
    Term "-" Expression
  Term:
    Primary
    Term "*" Primary
    Term "/" Primary
    Term "%" Primary
  Primary:
    Number
    "("Expression")"
  Number
    Floating-point-literal

Writing a grammer
1. Distinguish a rule from a token
2. Put one rule after another (Sequencing)
3. Express alternative patterns (alternation)
4. Express a repeating pattern (repetition)
5. Recognize Llte grammar mle to start with
